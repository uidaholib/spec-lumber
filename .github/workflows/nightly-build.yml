name: Scheduled build
on:
  schedule:
  - cron: '30 13 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger our build webhook on Netlify
      run: curl -s -X POST -d {} "{TOKEN}"
      env:
        TOKEN: ${{ secrets.RENDER_CRON_BUILD_HOOK }}
